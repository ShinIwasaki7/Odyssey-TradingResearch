# Codex レビュー規則ファイル（AGENTS.md）の設計: 過去 PR の指摘分類と、設計適合レビューの改善案

作成日: 2026-09-24
状態: **提案（人間の承認待ち）**。承認後に `AGENTS.md` を別 PR で追加する（CLAUDE.md「設計→合意→実装」）。
上位文書: [PR レビュー方針](../pr_review_policy.md)（レビュー手順の唯一の正本）、[D01](D01_architecture_and_dependency_rules.md)（依存規則の正本）、Codex レビューの道具立てを定めた決定記録 [ADR-0017](../decisions/0017-codex-review-tooling.md)
対象範囲: 本書が決めるのは **(1) AGENTS.md の役割と置き場、(2) その中身の草案、(3) 指摘分類から導いた設計・実装側の改善案** の3つ。レビューループの手順（巡数・終了条件・状態遷移）は本書の対象外で、PR レビュー方針 §4 を変えない。

## 0. 要点（先に読む）

- Codex が読むファイル名は **`AGENTS.md`** である。`AGENT.md` は認識されない（§1）。
- 過去 24 本の PR に対する Codex の指摘 **338 件**を分類した。**誤検知はほぼ無い**（不採用 1 件、設計差し戻し 2 件）。問題は精度ではなく、**同じ形の指摘が PR をまたいで再発すること**と、**設計の空欄が実装後に見つかること**である（§2）。
- したがって AGENTS.md の狙いは「指摘を減らす」ではなく、**再発している指摘の型を Codex に最初の巡で網羅的に出させ、範囲外・不採用の判定材料（設計文書の節）を指摘に添えさせる**ことに置く（§3）。
- 併せて、指摘の型から導いた **設計・実装側の根本対処 6 件**を提案する（§5）。AGENTS.md はレビューの網であり、網に掛かる前に設計で潰す方が安い。

## 1. 前提: Codex が AGENTS.md をどう使うか

OpenAI の公開文書（2026-09-24 時点）から、本設計に効く事実だけを挙げる。

| 事実 | 本設計への影響 |
|---|---|
| 認識するファイル名は `AGENTS.override.md` と `AGENTS.md` のみ。git ルートから変更ファイルの階層へ向かって順に読み、近いものが優先される | ルートに 1 本置く。`AGENT.md` という名前では読まれない |
| 合計 **32 KiB** を超えた分は読まれない | 草案は約 7 KB に抑え、方針文書や D01 の内容を写さず節参照にする |
| GitHub の PR レビューでは、`## Code Review Rules` 節（`###` で小分け）を規則として使う | 見出し名はこの英語表記に固定する。本文は日本語でよい |
| 良い規則の条件: 「結果に影響する・リポジトリ固有・安全な側の書き方（または例外）を示す・範囲が明確で長持ちする・CI で機械的に検査できる事柄は書かない」 | ruff / mypy / import-linter が止めるものは書かない。書くのは **設計文書との適合・決定論・先読み・記録の完全性**など、機械検査の外側 |

## 2. 過去 PR の指摘の分類（PR #1〜#24、Codex の inline 指摘 338 件）

集計元: GitHub API から取得した全 PR の review / inline / issue comment。1 指摘 1 行の分類表は本 PR には含めない（作業用。必要なら再生成できる）。

### 2.1 指摘の型（カテゴリ）

| 記号 | 型 | 件数 | 主な出どころ |
|---|---|---|---|
| C | **未定義の振る舞い・仕様の穴**（状態×出来事の空欄、境界条件、例外系が未決） | **192**（57%） | 設計文書 PR（#14〜#16、#21〜#24）。実装 PR でも 27 件 |
| B | 文書間の不整合・写し漏れ（版・節番号・境界表のずれ） | 48 | 設計文書 PR |
| E | 型・不変条件・値オブジェクト（Decimal 境界、frozen の中身可変、通貨不一致、空表の列型） | 31 | 実装 PR 17 件、D06 の型定義 14 件 |
| F | 決定論・順序（ダイジェスト入力の抜け・順序、処理点通し番号の重複、格納順依存） | 21 | 実装 14 件、設計 7 件 |
| J | エラー処理・堅牢性（例外で診断が消える、既存成果物の無検査上書き、主キー重複の黙殺） | 20 | 実装 13 件 |
| A | **設計文書と実装の不一致**（設計が要求する記録・manifest 項目・検証を実装が省略） | 18 | 実装 PR 14 件（うち PR #19 が 11 件） |
| D | 依存規則・層の違反 | 5 | 契約定義そのもの（PR #2、#4）。**コードの層違反は 0 件** |
| G / H | テスト不足 / ツール設定 | 3 | — |

### 2.2 PR 種別・重大度・結末

重大度は Codex が各指摘に付けた印で、P0 = 必ず直す最重大（設計違反・データ破壊・先読み・決定論の破壊）、P1 = 必ず直す重大（バグ・契約違反・テスト欠落）、P2 = 改善提案。定義の正本は PR レビュー方針 §3。

| 観点 | 内訳 |
|---|---|
| PR 種別 | 設計文書 244 件（72%）/ 実装 85 件（25%）/ 骨格・ツール 9 件 |
| 重大度（Codex の付与） | P0 3 件 / P1 198 件 / P2 137 件 |
| 結末 | **採用修正 315 件**（PR #6 の 9 件は PR コメントに返信が無いが、修正コミット 3 件で対応済みと確認）/ 仮置き・後続 PR へ持ち越し 15 件 / 設計差し戻し 2 件 / 不採用 1 件（承認済み決定 Q8 と衝突、PR #14）/ 判定不能 5 件（PR #17 の収束後再依頼分） |

読み取り:

1. **Codex の指摘はほぼ全て正しい**。誤検知対策の規則（「これは指摘するな」）は最小限でよい。
2. **依存規則違反は import-linter が止めている**。AGENTS.md に依存方向の規則を書き直す必要はなく、D01 を参照させれば足りる。
3. **実装 PR の指摘の 3 分の 1 は「設計が要求する記録・項目・検証の省略」**（A）で、これは設計文書の表とコードを突き合わせれば機械的に見つかる型である。Codex に「設計文書の該当節を引用して不一致を述べる」形を要求すると、最初の巡で網羅される。
4. **設計文書 PR の指摘は「空欄」**（C）に集中し、PR #21 は空欄が残り続けて 15 巡以上かかった。レビュー方針 §4.2 が「状態×出来事の表で空欄をゼロにする」を根本対処として学んでいる。AGENTS.md には「表の空欄・境界表・ダイジェスト入力表の欠落を指摘する」規則として書き、設計側には表を必須にする改善（§5 R1）を置く。

### 2.3 PR をまたいで再発した根本原因（出典 PR 付き）

| # | 再発した形 | 出典 |
|---|---|---|
| RC1 | **ダイジェスト／ハッシュの入力に、新しいフィールドやタプルの順序正規化を含め忘れる**（設定ダイジェストに銘柄・カレンダー・口座が未反映、部品順序が未正規化、格納順で結果ダイジェストが変わる） | #3、#12、#14、#16、#19、#20 |
| RC2 | **複数部品をまたぐ値（取引機会 ID・区間・建玉 ID・run_id）の伝播規則が設計に無く、記録に列が入らない／不一致が通る** | #14、#15、#16、#19、#20 |
| RC3 | **設計が要求する記録・snapshot・manifest 項目を実装が省略する**（根拠記録の市場／口座 snapshot、result.json の能力検査結果、空表のスキーマ） | #19、#20 |
| RC4 | **失敗・欠損が結果に残らない**（読み出し失敗で検査結果が空のまま評価が完了、例外で診断が消える、主キー重複が後勝ちで黙殺） | #7、#20 |
| RC5 | **既存成果物を無検査で上書きする**（承認済み snapshot、run 成果物） | #10、#19 |
| RC6 | **エンジンの処理順序・通し番号**（足欠損検査が期限切れ処理より後、RUN_END と約定後フェーズで処理点通し番号が重複、決済要求にエントリー用遅延を適用） | #19 |
| RC7 | **境界表（§1.2）・他文書への反映漏れ**が巡をまたいで再発 | #15、#16、#21、#22 |

## 3. AGENTS.md の設計方針

### 3.1 役割分担（重複を作らない）

PR レビュー方針は 2026-09-23 に「レビューの規則は本書だけを直す」と決めた。4 文書に同じ規則を写して収束しなかった反省である。AGENTS.md はこれに従い、**手順を一切書かない**。

| 文書 | 決めること | AGENTS.md との関係 |
|---|---|---|
| PR レビュー方針 | 依頼の仕方、対象範囲の決め方、3 分類と P0〜P2、巡数、終了条件、merge 前提 | AGENTS.md は §3・§3.1 を**参照するだけ**（Codex が指摘に付ける優先度の定義として） |
| D01 | 依存規則・層・ポート・外部ライブラリの配置 | AGENTS.md は「import-linter の外側で確かめること」だけ書き、規則本体は D01 §2.2・§4・§8 を参照 |
| D02〜D08 | 各型・表・手順 | AGENTS.md は「設計文書の該当節を引用して不一致を述べよ」と要求する。個々の型は書かない |
| **AGENTS.md（新設）** | **Codex（と設計適合レビューを行う Claude）が何を・どの形で指摘するか** | 両レーン共通の「レビュー観点の正本」 |

### 3.2 両レーンで同じ規則を使う

Codex は現在「リポジトリ用の環境を作成してください」と返して利用できず、Sonnet の設計適合レビューで代替している（progress.md）。AGENTS.md の `## Code Review Rules` を **Claude の設計適合レビュー（方針 §5）でも読ませる**ことで、代替期間中も同じ観点で検査でき、Codex 復帰後に観点が食い違わない。方針 §5 に「観点は AGENTS.md を参照」の 1 行を足す改訂を、AGENTS.md 追加の PR に含める。

### 3.3 置き場・言語・大きさ

| 項目 | 推奨 | 代替案 |
|---|---|---|
| 置き場 | **ルートに 1 本** | パッケージ別に `src/odyssey_fx/<pkg>/AGENTS.md` を足す（近いものが優先される）。今は規則が 7 KB で足りるため不要。ある節が 3 KB を超えたら分割を検討 |
| 言語 | **日本語本文＋英語の固定見出し**（`## Code Review Rules`、`###` 小見出しに英語の型名） | 全文英語。Codex は日本語を読めるので、リポジトリの他文書と揃える方が保守しやすい |
| 大きさ | 12 KB 以下を維持（上限 32 KiB の 4 割） | — |
| 更新の契機 | **同じ型の指摘が 2 本以上の PR で再発したとき**に規則を 1 つ足す。規則には出典 PR 番号を残す | 各 PR ごとに足す（膨らむ） |

### 3.4 書き方の規則

- 各規則は「**何を確かめるか → 安全な側はどちらか → 根拠の設計節**」の順で 1〜3 行。
- ruff / mypy / lint-imports / pytest が止めるものは書かない。
- 「指摘するな」型の規則は、**範囲外・不採用の判定を Codex 側に求めない**ために書く（判定は方針 §3.1 で作業役が行う）。Codex には「範囲外なら担当文書名を添えて 1 行にまとめよ」とだけ求める。

## 4. AGENTS.md 草案（承認後にこのままルートへ置く）

````markdown
# AGENTS.md

このリポジトリは FX 戦略研究基盤（Python 3.12、`src/odyssey_fx/`）。
**設計文書が正本**であり、コードは `docs/design/D01〜D08` と `docs/decisions/`（ADR）に従う。
設計への異論は PR で直さず「要決定」として人間に上げる（`docs/pr_review_policy.md` §1・§4.3）。

- 検査は CI と同じ並び: `uv run ruff check .` / `uv run ruff format --check .` / `uv run mypy` / `uv run lint-imports` / `uv run pytest`
- 依存規則の正本は D01 §6。`pyproject.toml` の import-linter 契約はそれを実装した検査であり、契約の追加・緩和には D01 の改訂が要る（D01 §12）。層の意味は D01 §2、ポートの所在は D01 §4
- 用語: 「範囲表」= PR 本文のレビュー対象範囲。「仮置き」= 設計に無い振る舞いを実装が決めた箇所（方針 §6）

## Code Review Rules

### Scope and reporting format

- 指摘は PR 本文の「レビュー対象範囲」（設計文書の §1.2 境界表、または PR 本文の範囲表）の**中に限る**。範囲外の事項は原則報告しない。報告する価値があるもの（データ破壊・先読み・承認済み設計との衝突）だけ、**P2 の指摘として本文の先頭に「範囲外候補（担当: 文書名）」と書く**。分類と扱いは作業役が `docs/pr_review_policy.md` §3.1 で行う。
- 各指摘に **具体的な失敗シナリオ**（入力→誤った出力）を添える。設計適合の指摘（「設計と違う」「設計に無い」）には **根拠となる設計文書の節**（例: `D06 §4.2 手順 6`）も添え、根拠節を示せない設計適合の指摘は P2 とする。**再現可能な実装バグ（通常入力で例外・誤った値）は設計節の有無に関わらず P1** である（方針 §3）。
- 優先度（P0 / P1 / P2）の定義は `docs/pr_review_policy.md` §3 の表を読んで付ける。ここには写さない。
- 同じ型の指摘が複数箇所にあるときは、代表 1 件に他の箇所を列挙してまとめる。
- 承認済みの ADR・設計文書の決定に反する提案はしない。設計側に直すべき点があるなら、**P2 の指摘として本文の先頭に「要決定候補」と書く**（設計を変える判断は人間が行う。方針 §4.3）。

### Conformance to the approved design (A)

- 設計文書が定める **記録の表・列・主キー**（判断履歴の表は D06 §9.2。段階2 で 15 表、段階3 で 19 表。run manifest は D06 §9.3、result.json は D06 §9.4、評価 5 表と評価 manifest は D07 §8）がすべて書かれるか。**0 行でも列とその型が残る**か（PR #19）。
- manifest・ダイジェスト・根拠記録に、設計が列挙する項目（市場／評価／口座 snapshot、能力検査結果、換算経路、下位足の参照）がすべて入るか（PR #19）。
- 設計のフェーズ順（D06 §4.1 の rank 順・§4.2 の手順番号）どおりに処理されるか（例: 足欠損検査は期限切れ処理より前）（PR #19）。
- コンパイル後に宣言の制約（warmup 等の時刻制約、有効性束縛）が失われないか（PR #18）。
- 実装が設計に無い振る舞いを決めていたら、PR 本文の仮置き一覧に載っているかを確かめ、無ければ P1 で「仮置き未記載」と指摘する。

### Determinism and reproducibility (F)

- **ダイジェスト／ハッシュの入力**が、設計文書の入力一覧と一致するか。新しいフィールドの取りこぼし、タプル・集合の順序未正規化、格納順依存を疑う（PR #14・#16・#19・#20）。
- `ProcessingPoint` の通し番号が run 内で一意か。フェーズ跨ぎ（RUN_END、約定後）で重複しないか（PR #19）。
- domain / application が実時計・乱数・環境変数を読んでいないか（D01 §2.2 規則 2）。
- 主キーの重複が黙って後勝ちにならないか。重複判定が型変換後の値で行われるか（PR #20）。

### Lookahead and time (P0)

- 判断時点で公開されていない情報（`available_at` より後の足、未承認 manifest、未記録 partition）を参照していないか（PR #7）。
- 公開遅延・DST 境界・週末／祝日の扱いが D03 の規則と一致するか。エントリー用の遅延を決済要求に流用していないか（PR #19）。

### Types, invariants and numerics (E)

- `Price`/`Quantity`/`Money`・台帳は Decimal、Feature は float。`Decimal(float)` と、グローバルな Decimal context への依存を禁じる（ADR-0012、D02）。
- 通貨・単位が異なる値の演算が素通りしないか（手数料通貨、口座通貨と決済通貨）（PR #19）。
- `frozen=True` の dataclass の中身（list / dict）が可変のまま公開されていないか（PR #6）。
- 売買方向で参照価格（bid / ask）とスプレッドの適用が対称か。二重加算が無いか（PR #19）。
- 検証の正規表現が `fullmatch` か。`$` は末尾改行を許す（PR #6）。

### Missing data, failures and overwrites (J)

- 構造エラーは例外で止め、入力欠損は `MissingInputPolicy` で扱う。**例外を欠損に読み替えない**（D01 §2.2 規則 5）。
- 読み出し失敗・null・型不一致は、**検査結果の表に観測値として残す**。例外で診断が消えて評価が「完了」にならないか（PR #20）。
- 既存の成果物を**検査せずに上書きしていないか**。既定は「存在すれば失敗」。**確定済み snapshot は常に失敗**（D03 §3.7.1）。**run 成果物 `runs/<run_id>/` は明示の置換指示（`replace=True`）があり、旧 manifest を残す場合だけ置換できる**（ADR-0006）。この正当な置換経路を違反として報告しない（PR #10・#19）。
- ID 不一致・能力検査の失敗が、書き込みの**前**に検出されるか（PR #19・#20）。

### Architecture beyond import-linter (D)

- DataFrame・`numpy.ndarray` 等の外部ライブラリ型が domain / application の引数・戻り値・記録に出ていないか（D01 §2.2 規則 1）。
- ポートは利用側の application が `Protocol` で定義し、実装者はそれを import せず構造的に満たすか（D01 §4）。**例外**: 実際の利用側が application より下の層なら、構造をその層で定義し application が同じ名前で再公開する（D01 §4 v2.4。`backtest.engine.loop` の `PublicationFeed` / `ExecutionSeries` / `IntrabarSeries` / `Calendar` が該当）。この形を「application に定義されていない」と指摘しない。
- `adapters` / `application` の型を `__init__.py` で再エクスポートしていないか（D01 §8）。
- Pydantic モデルが `app.config` の外へ流出していないか（D01 §2.2 規則 6）。

### Tests (G)

- 設計の各規則（状態遷移・不変条件・手順）に対応するテストが `tests/` の該当層（D01 §9、D08）にあるか。
- golden / property テストが**実際に値を比較している**か。比較を省略して通しているテストを指摘する（PR #20）。
- 受入テストが人工データで受入れ→run→評価を 1 本通しているか（D08）。

### Design-document PRs

- **状態×出来事の表に空欄が無いか**。起きない組み合わせにも「到達しない（理由）」があるか（方針 §4.2、PR #21）。
- §1.2 境界表（決めること／委ねること／受け取ること）と本文が一致するか。表の外の事項を本文が決めていないか。
- 複数部品をまたぐ値（機会 ID・区間・建玉 ID・run_id）の**伝播規則と記録先**が書かれているか（PR #14〜#16）。
- ダイジェストの入力一覧、記録表の主キー、空表の扱いが書かれているか。
- 改訂した文書の版・節番号を参照している他文書（D01、README、ADR）が同じ PR で追随しているか。
- 段階3 以降で新しく許可した振る舞い（待機・遡り・確認）の受け口・記録先・主キーが揃っているか（PR #22）。

### Do not report

- ruff / mypy / lint-imports が検出する事項、命名・スタイルのみの事項。
- レビュー対象範囲の外（上の「範囲外候補」を除く）。
- 承認済み設計と衝突する変更提案（上の「要決定候補」を除く）。
````

## 5. AGENTS.md の外で行う改善提案（設計・実装側の根本対処）

レビュー規則は「掛かった魚を逃さない網」であり、再発の原因は設計・実装側にある。指摘の型ごとに、根本対処の選択肢と推奨を挙げる。**いずれも人間の承認後に個別の PR で行う**。段階3 実装（5 PR）と衝突しない順に並べた。

| # | 対象の型 | 提案（推奨を先頭） | 影響 | 規模 |
|---|---|---|---|---|
| R1 | C（空欄）、RC7 | **設計文書テンプレートに必須表を 3 つ加える**: §1.2 境界表（既存）、状態×出来事表、値の伝播表（値・生成元・記録先・主キー）。D04 以降の改訂時にこの 3 表の有無を設計適合レビューが確かめる。代替: 表を任意のままとし AGENTS.md の規則だけで拾う | 設計文書 PR の巡数が減る。PR #21 型の長期化を防ぐ | 文書テンプレートの追記のみ。約 1 時間 |
| R2 | F、RC1 | **ダイジェスト入力を 1 か所に宣言する**: 各ダイジェスト（設定・結果・snapshot・部品）の入力フィールド一覧を、対象 dataclass のそばに定数として持ち、「dataclass の全フィールド − 除外一覧 = 入力一覧」を property テストで固定する。フィールドを足すとテストが落ち、除外か採用かを設計判断として書かせる。代替: 現状のままレビューで拾う | RC1（6 PR で再発）を機械検査に変える。D02 §9（正規化エンコード）の改訂が要る | D02 改訂＋テスト 1 種。半日 |
| R3 | A、E、RC3 | **記録表のスキーマを domain の宣言にする**: 判断履歴の表（段階2 で 15 表、段階3 で 19 表）・評価 5 表の列名と型を `backtest.trace` / `evaluation.domain` の宣言（frozen dataclass の一覧）として置き、書き手（`fs_store`）と読み手（`evaluate_run` の整合検査 C1〜C8）が同じ宣言を使う。0 行でも列が残る。主キー重複検査が 3 表だけだった問題（PR #20）も宣言から全表へ広がる | RC3・RC4 の半分を構造で防ぐ。D06 §9.2（判断履歴の表）・D07 §10.2（整合検査）の改訂が要る | 段階3 PR 5/5（エンジン＋受入）の後。1〜2 日 |
| R4 | J、RC5 | **成果物の書き込みを「存在すれば失敗」に統一する**: `evaluation.adapters.fs_store` と `marketdata.adapters.parquet_store` に共通の書き込み前検査（存在確認と ID 一致）を置く。**確定済み snapshot は常に失敗**（D03 §3.7.1 の確定事項。承認やアクセス記録は snapshot ID に含まれないため、置換すると承認済み成果物を失う。統合テスト `test_classify_does_not_overwrite_a_settled_snapshot` が固定済み）。明示フラグによる置換を許すのは **run 成果物（`runs/<run_id>/`）だけ**。代替: 各 CLI で個別に検査（現状） | RC5 の再発防止。D06 §9.2 の追記。D03 は変えない | 半日 |
| R5 | J、RC4 | **「観測値として残す」を検査結果型の規則にする**: 整合検査（C1〜C8）と能力検査の結果型に「読めなかった」区分を必ず持たせ、例外で検査を抜ける経路を無くす。D07 §10.2 の追記 | PR #20 型の再発防止 | 半日 |
| R6 | F、RC6 | **エンジンのフェーズ順序と処理点の採番を表で宣言する**: D06 §4.2 の手順表を、`backtest.engine` が読む順序定義（列挙）に写し、通し番号の発番を 1 か所（`ProcessingPoint` の生成器）に集める。代替: 現状の手続き的順序のまま AGENTS.md で拾う | RC6 の再発防止。段階3 PR 4/5・5/5 で扱う内容と重なるため、その PR 内で同時に検討する | 段階3 PR 5/5 に統合 |

D（依存規則）については追加提案なし。import-linter が機能しており、コードの層違反は 0 件だった。

## 6. 要決定（人間に決めてもらうこと）

| # | 決めること | 選択肢（推奨を先頭） | 結果への影響 |
|---|---|---|---|
| Q1 | AGENTS.md の置き場 | **(a) ルート 1 本** / (b) ルート＋パッケージ別 | (b) は近い規則が優先されるため精度は上がるが、32 KiB 上限の管理と文書の重複が増える |
| Q2 | 言語 | **(a) 日本語本文＋英語見出し** / (b) 全文英語 | (b) は Codex の規則解釈がわずかに安定する可能性があるが、他文書と言語が割れ保守が重くなる |
| Q3 | 設計適合レビュー（Claude レーン）でも同じ規則を読むか | **(a) 読む（方針 §5 に 1 行追記）** / (b) Codex 専用にする | (a) は代替期間中も同じ観点で検査できる。(b) は観点が 2 系統になる |
| Q4 | §5 の根本対処 R1〜R6 の採否 | **(a) R1・R2・R4・R5 を承認し、R3・R6 は段階3 PR 5/5 の後に再提案** / (b) 全件承認 / (c) AGENTS.md のみ | (a) は段階3 実装と衝突しない範囲で先に効く。(b) は D06・D07 の改訂が段階3 と競合する |
| Q5 | ADR を起こすか | **(a) ADR-0034「レビュー観点は AGENTS.md を正本とし両レーンで共有する」を追加** / (b) 本書と方針 §5 の追記のみ | (a) は ADR-0017 との関係が記録に残る |

## 7. 承認後の導入手順

1. `AGENTS.md` をルートに追加（§4 の草案をそのまま）。同じ PR で PR レビュー方針 §5 に「観点は AGENTS.md を参照」を 1 行追記（Q3 が (a) の場合）、Q5 が (a) なら ADR-0034 を追加。
2. `.claude/skills/pr-review/SKILL.md` の手順 4 は変えない（依頼本文の書き方は方針 §3.1 のまま）。
3. Codex の環境が作られたら、次の実装 PR（段階3 PR 1/5 以降）で **最初の巡の指摘件数と型**を本書 §2 の分布と比べ、規則の効き目を記録する。効いていない型があれば §3.3 の更新契機に従って規則を足す。
4. R1〜R6 は Q4 の決定に従い、それぞれ独立の PR で行う。
