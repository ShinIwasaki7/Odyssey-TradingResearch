"""strategy のコンパイラ（D05 §5）。

宣言（戦略の「形」）を、ランタイムがそのまま実行できる `CompiledStrategy` へ変換する。
検査に落ちた宣言は例外ではなく `CompileFailed` で返し、**どの検査でどの宣言のどこが原因
か**を必ず添える。黙って無視する経路を作らない（D04 §12）。

モジュール構成（D05 §2）:

- `compiled`: 解決済みの型（`CompiledStrategy` ほか）と、拒否の区分・誤りの表現
- `graph`: 依存グラフ（明示辺＋エンジン上の因果辺）・循環検出・評価順
- `capability`: 対応しない構成の拒否（能力検査）と、待機・遡り・出力の履歴窓の組合せ検査
- `hashing`: 解決済み設定の内容ハッシュ
- `validate`: 10 段の手順

サブモジュールは明示的に import して使う。
"""
