# CLAUDE.md

## 絶対ルール

- **前提を silent に決めない**: 解釈が割れる/不明なら止めて問うこと。
- 設計をドキュメントに起こしてから人間の合意を得た上で実装を行なってください。
- 設計は `docs/design/` 配下にドキュメントを残してください。
- **人間向けの言葉を優先する**: 報告・質問・PR本文では、内部識別子・略語・専門用語を単独で使わず、初出時に「何を指すか・なぜ必要か・何に影響するか」を平易な日本語で先に説明して正式な識別子を括弧書きし、以後も意味の分かる短い名称を用いること。

## 出力スタイル（常時適用）

すべての人間向け応答に、次の出力スタイルを適用する。

@.claude/skills/i-have-adhd/SKILL.md

## 並行作業（worktree 自動隔離）

複数セッションを別ブランチで安全に並行させるため、コミットを生むタスクを始めるなら、開始ブランチが何であれ`EnterWorktree` で `.claude/worktrees/<task>` に隔離する（base ref=origin/main・`worktree.baseRef=fresh`）。判断は二択: **コミットを生む→隔離 / read-only（質問・調査・分析・PR レビュー・相談）→現ディレクトリのまま**。

- 完了/中断時は `ExitWorktree`（keep=再開 / remove=破棄）。PR は worktree から `gh pr create` まで完結可。
- **実装後の Codex review ループは承認停止なしで自走すること。push→PR→`@codex review`→**4分間隔ポーリング**→修正→再依頼を都度承認なしで進め、**人間に確認するのは merge 前のみ**。**基本2巡・round1 で P0/P1 が0件なら2巡目省略**・最大2巡。

## 人間向け説明の用語ルール

人間向けの報告・質問・PR 本文・レビューコメントでは、認知的負荷を下げるため次を守る。

- 内部識別子（契約 ID の F5c、決定記録の ADR-0006、設計文書の D01〜D07、要決定項目の B-5 / C-4、指摘優先度の P0 / P1 など）を**単独で説明に使わない**。最初に内容が分かる日本語名を書き、その後に識別子を括弧書きする。
  - 悪い例: 「F5c を pyproject に追加します」
  - 良い例: 「設定パーサーの直接 import を app.config 以外で禁止するルール（F5c）を pyproject に追加します」
- 2回目以降も、原則として「設定パーサー集約ルール」のような意味が分かる短い名称を使い、識別子は CI・設計書・レビューコメントを照合するための補助として扱う。
- 決定記録の番号、設計文書の番号、要決定の記号、指摘の優先度も、初出時には必ず意味を平易な日本語で説明する（例: 「決定記録（ADR）」「重大な設計違反（P0）」）。
- 人間に判断を求めるときは、内部番号ではなく「何を決める必要があり、結果にどう影響するか」を先に書く。
- CI や設計書が参照する正式な契約 ID・契約名・ADR 番号・文書番号そのものは変更しない。このルールは説明の書き方に関するものであり、識別子の廃止ではない。

